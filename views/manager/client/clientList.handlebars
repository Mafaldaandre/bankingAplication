<div class="card">
    <div class="card-header">
        <h5 class="card-title">Lista de clientes</h5>
    </div>

    <div class="list-group list-group-flush">
        {{#if clients}}
            {{#each clients}}
                <a href="/manager/client/clientList/{{ this._id }}" class="list-group-item list-group-item-action">
                    {{ this.name }}
                </a>
            {{/each}}
        {{^}}
            <div class="list-group-item">Não existe clientes</div>
        {{/if}}
    </div>
</div>